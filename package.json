{
  "name": "git_task_interactive_products_and_services",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "Micha Kodalle <contact@micha-kodalle.de>",
  "scripts": {
    "build": "parcel build index.html && npm run build_css",
    "build_css": "npx tailwindcss -i ./css/tailwind.css -o ./css/tailwind_out.css --watch",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "tailwindcss": "^3.1.8"
  }
}
